@model IEnumerable<DownloadDTO>

<ul id="download-list">
    @foreach(var download in Model)
    {
        <li>
            <a href="@download.DownloadUrl" target="_blank"></a>
            <i class="material-icons">save_alt</i>
            <span>&nbsp;@download.DownloadTitle</span>
        </li>
    }
</ul>